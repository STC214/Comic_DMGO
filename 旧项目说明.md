# 旧项目迁移说明

本文说明原 `F:\Project\01_Comics_Images\comic_downloader` 与当前 Go 重构项目之间的关系。这里记录的是迁移背景，不再作为当前启动手册；当前操作请以 [README.md](README.md) 为准。

## 1. 保留的内容

当前项目继续复用旧项目的：

- Windows/Fyne UI 结构。
- 任务创建、暂停、重试和历史记录模型。
- 下载目录和任务详情展示方式。
- 真实 Chromium、持久 profile、人工验证后继续下载的总体思路。
- 已验证可工作的 Chromium 运行文件。

## 2. 已替换的内容

旧项目曾使用 Python、DrissionPage 和 worker 脚本完成 `myreadingmanga`。当前项目已经替换为 Go 双线路：

- `myreading_worker.go`：统一下载流程。
- `myreading_rod.go`：Rod 线路。
- `myreading_playwright.go`：Playwright-Go 线路。

当前 `myreading` 全流程不再依赖：

- Python 解释器。
- DrissionPage。
- `workers/myreadingmanga_drissionpage/main.py`。
- `workers/download_middleware.py`。
- `tools/browser_launch.py`。

因此旧项目中的 Python 启动、`compileall`、Python 环境变量和 Python worker 命令均已废弃。

## 3. 验证流程变化

旧流程可能在验证成功后关闭可见浏览器，再创建新的后台浏览器。当前流程改为：

1. 直接启动真实有头 Chromium。
2. 窗口在显示首帧前保持桌面外和隐藏状态。
3. 检测到验证才显示同一个窗口。
4. 用户完成验证后隐藏同一个窗口。
5. 保留页面、Cookie、缓存和 CDP 会话继续采集与下载。

这样避免验证状态跨浏览器切换造成丢失，也避免正常任务启动时浏览器窗口闪烁。

## 4. 图片下载变化

当前实现保留旧项目的浏览器会话优先思路，并在 Go 中完成：

- 浏览器资源读取。
- Cookie 和 Referer 继承。
- HTTP 重试回退。
- `.part` 临时落盘。
- 图片解码校验。
- 断点续传。
- 实时传输速度。

浏览器获取图片时，Rod 和 Playwright 都通过 CDP 监听实际网络字节；HTTP 回退在响应体复制过程中报告字节。

## 5. 浏览器来源

开发阶段仍可参考旧项目 Chromium：

```text
F:\Project\01_Comics_Images\comic_downloader\runtime\chromium\chrome.exe
```

但便携包运行时最高优先级是自身目录：

```text
<便携包>\runtime\chromium\chrome.exe
```

所以移动到其他机器后不会依赖上述开发机绝对路径。

## 6. 历史记录

UI 仍支持导入旧 `comic_downloader_state.json`：

- 只保留 `myreadingmanga` 任务。
- 按规范化 URL 去重。
- 导入的排队或运行任务恢复为暂停状态，不自动开始下载。
- 修复已知的状态文本乱码。
- 旧站点或未知站点任务跳过。

## 7. 当前项目布局

- `main.go`：任务、持久化和运行根目录。
- `site_backend.go`：站点路由和结果处理。
- `fyne_ui_windows.go`：Windows UI。
- `myreading_worker.go`：Go 下载核心。
- `myreading_rod.go`：Rod/CDP。
- `myreading_playwright.go`：Playwright-Go/CDP。
- `runtime\chromium`：便携 Chromium。
- `runtime\playwright\driver`：便携 Playwright 驱动。
- `download`：默认下载目录。
- `logs`：日志目录。
- `runtime\browser-profiles`：验证基线和临时任务 profile。

## 8. 当前构建方式

```powershell
go test ./... -count=1
go test -race ./... -count=1
go vet ./...
.\build.ps1
```

便携包：

```powershell
.\package-portable.ps1 -Version <14位版本号>
```

当前成品为：

```text
dist\Comic_PC_Portable_20260812105826.zip
```

该包已包含 Chromium、Playwright 驱动和 Node.js，不需要旧项目目录或目标机器开发环境。
