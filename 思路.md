## 2026 年最新最强方案（Go 版）

### 1. 核心思路
最强方案不是单独一个库，而是把 Go 端当“真实浏览器容器”，然后叠加：
- 真实 Chrome/Chromium
- 持久浏览器 profile
- 深度 anti-detect
- 高质量 residential/mobile 代理
- 真实用户行为模拟
- CAPTCHA 解码链路

---

### 2. 推荐栈

- 主库：`github.com/go-rod/rod`
- 反检测：`github.com/go-rod/stealth`
- 备选：`github.com/playwright-community/playwright-go`（如果你要更稳定的 Playwright 生态）

---

### 3. 今年最新更强点

1. 真实浏览器模式
   - 优先 `Headless(false)` 或 `--headless=new`
   - 避免传统纯 headless 被 Cloudflare 识别

2. 持久 profile
   - `--user-data-dir=./profile`
   - 保留 Cookie、localStorage、缓存
   - 复用已通过验证的状态

3. 深度 anti-detect
   - `navigator.webdriver = false`
   - 填充 `window.chrome`
   - 自定义 `navigator.languages`、`navigator.plugins`
   - 修复 `permissions.query`
   - 注入 `navigator.deviceMemory`、`navigator.hardwareConcurrency`、`navigator.platform`

4. 高质量代理
   - residential / mobile proxy
   - 建议 `socks5://user:pass@ip:port`
   - 避免 datacenter IP / 共享代理

5. 行为模拟
   - 滚动、点击、鼠标移动、键盘输入
   - 随机延迟、随机等待
   - 先加载页面再抓取

6. CAPTCHA 处理
   - 遇到 Cloudflare challenge 或 captcha 时，自动进入解码流程
   - 外部打码服务或人工干预

---

### 4. 最强 Go 示例

```go
package main

import (
    "log"
    "math/rand"
    "time"

    "github.com/go-rod/rod"
    "github.com/go-rod/rod/lib/launcher"
    "github.com/go-rod/stealth"
)

func randSleep(min, max int) {
    time.Sleep(time.Duration(min+rand.Intn(max-min)) * time.Millisecond)
}

func injectAntiDetect(page *rod.Page) {
    _, _ = page.Eval(`() => {
        Object.defineProperty(navigator, 'webdriver', { get: () => false });
        Object.defineProperty(window, 'chrome', { get: () => ({ runtime: {} }) });
        Object.defineProperty(navigator, 'languages', { get: () => ['zh-CN', 'zh', 'en-US', 'en'] });
        Object.defineProperty(navigator, 'plugins', { get: () => [1,2,3,4,5] });
        Object.defineProperty(navigator, 'platform', { get: () => 'Win32' });
        Object.defineProperty(navigator, 'hardwareConcurrency', { get: () => 8 });
        Object.defineProperty(navigator, 'deviceMemory', { get: () => 8 });
        const orig = navigator.permissions.query;
        navigator.permissions.query = (params) => {
            if (params.name === 'notifications') {
                return Promise.resolve({ state: Notification.permission });
            }
            return orig(params);
        };
    }`)
}

func main() {
    rand.Seed(time.Now().UnixNano())

    launcherURL := launcher.New().
        Headless(false).
        Set("disable-blink-features", "AutomationControlled").
        Set("disable-infobars", "").
        Set("lang", "zh-CN,zh;q=0.9,en;q=0.8").
        Set("user-agent", "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36").
        Set("window-size", "1366,768").
        Set("no-sandbox", "").
        Set("disable-dev-shm-usage", "").
        // Set("proxy-server", "socks5://user:pass@host:port").
        UserDataDir("./rod-profile").
        MustLaunch()

    browser := rod.New().ControlURL(launcherURL).MustConnect()
    defer browser.MustClose()

    page := browser.MustPage()
    stealth.MustPage(page)
    injectAntiDetect(page)

    page.MustNavigate("https://www.drissionpage.cn/browser_control/connect_browser/#-%E7%94%A8%E7%A8%8B%E5%BA%8F%E5%90%AF%E5%8A%A8%E7%9A%84%E6%B5%8F%E8%A7%88%E5%99%A8")
    page.MustWaitLoad()

    randSleep(800, 1600)
    page.MustEval(`window.scrollBy(0, window.innerHeight/3)`)
    randSleep(800, 1600)
    page.MustEval(`window.scrollBy(0, window.innerHeight/2)`)

    html, err := page.HTML()
    if err != nil {
        log.Fatal(err)
    }

    log.Printf("抓取成功，页面长度=%d", len(html))
}
```

---

### 5. 额外建议

- 如果你要对付 Cloudflare Bot Management，最好结合：
  - 请求队列 + 重试逻辑
  - 状态检测：`Verification Required` / `cf_chl_...`
  - 浏览器截图与日志
- 如果要做批量，建议：
  - 每个 session 用独立 profile
  - 复用已经通过的 profile
  - 避免频繁切换同一 IP

---

### 6. 最强结论

今年最强的 Go 方案是：

- `rod + stealth`
- 真实可视浏览器
- 持久 `user-data-dir`
- 高质量 residential/mobile 代理
- 深度 anti-detect 注入
- 真实人类行为
- CAPTCHA 解码链路

> 这才是 2026 年你要的“最新最强”方案。

---

### 核心验证（过验）与后台下载流程

下面把项目中“过验（verification）”与“后台下载”两条核心线索按步骤汇总，并给出主要代码位置以便快速定位：

- **验证流程（概览）**
    - 入口：任务启动函数 `runGoMyreadingTask()`（见 [myreading_worker.go](myreading_worker.go)）。
    - 页面快照与 DOM 抽取：使用内嵌 JS `myreadingDOMSnapshotJS`，由 `Snapshot()`/`CurrentSnapshot()` 返回 `myreadingSnapshot`（见 [myreading_worker.go](myreading_worker.go) 与浏览器实现）。
    - 验证检测：`snap.Verification` 或 `isMyreadingVerificationSnapshot()`（匹配 Cloudflare/turnstile 标题或页面文本）。
    - 人机交互：若检测到验证，调用 `browser.SetVisible(true)` 并把任务状态设为 `TaskWaitingVerification`，通过轮询 `CurrentSnapshot()` 等待用户在可见浏览器完成验证，验证成功后继续抓取（实现详见 [myreading_worker.go](myreading_worker.go)）。
    - 恢复路径：后台/守护场景下的恢复函数 `recoverMyreadingVerification()`（见 [site_backend.go](site_backend.go)），用于在守护进程中把等待的任务恢复到可见浏览器以便完成过验。

- **后台下载流程（图片采集）**
    - 图片来源：通过页面快照收集 `snap.Images`（多页合并后形成图片 URL 列表），最后调用 `downloadMyreadingImages()` 发起下载（见 [myreading_worker.go](myreading_worker.go#L489)）。
    - Cookie 会话：先从浏览器 `Cookies()` 填充 `http.Client` 的 `cookiejar`，保持会话一致性。
    - 浏览器优先：若可用，优先调用 `browser.Resource(ctx, rawURL)` 由浏览器通过 CDP/页面加载直接获取资源（参见 [myreading_playwright.go](myreading_playwright.go) / [myreading_rod.go](myreading_rod.go) 的 `Resource` 实现）；成功则用 `saveMyreadingImageBytes` 保存。
    - HTTP 回退与重试：浏览器取不到时使用带 `Referer`/`User-Agent` 的 `http.Client` 发 GET 请求，并根据 `retries` 重试；响应需为 2xx 才继续。
    - 临时写入与校验：文件先写入 `.part` 临时文件，然后走 `validateDownloadedImage()`（拒绝 `text/html`、`json` 等非图片内容，并校验像素尺寸），通过后重命名为最终文件（命名格式 `0001.jpg/0002.png`）。
    - 错误与续传：已存在且校验通过的文件会被重用以支持重试/续传；若某张图连续失败，函数返回错误并中止，上层任务记录 `TaskError`。
    - 进度回调：每张图片完成后调用进度回调 `progress(done,total,bytes,transferred)` 更新状态。

- **测试与边界情况**
    - 单元测试：`myreading_worker_test.go` 包含端到端保存、HTML 响应拒绝、取消处理等测试，便于回归验证。

**简要流程图（Mermaid）**

```mermaid
graph TD
    A[启动任务 runGoMyreadingTask] --> B[创建/复用 Chromium profile]
    B --> C[打开页面 Snapshot]
    C --> D{检测 Verification?}
    D -- 是 --> E[SetVisible(true) 等待用户完成验证]
    E --> C
    D -- 否 --> F[收集 images 列表]
    F --> G[调用 downloadMyreadingImages]
    G --> H{浏览器 Resource 成功?}
    H -- 是 --> I[saveMyreadingImageBytes -> 保存]
    H -- 否 --> J[HTTP 下载 -> .part -> 验证 -> 重命名]
    I --> K[进度更新]
    J --> K
    K --> L[任务完成或错误上报]
```

如果你需要，我可以：
- 把本节内容再精简为一页 README 可打印的步骤清单；
- 或把流程图导出为 PNG（需额外工具）。
