# Comic PC Go 重构设计与当前状态

## 1. 目标

重构 `myreading` 下载链路，同时直接复用原 `comic_downloader` 的 Windows UI 和任务模型。

当前目标已经落地：

- Go 负责页面采集、验证等待、图片获取、保存、速度统计和资源回收。
- 保留 Rod 与 Playwright-Go 两条线路。
- 能完成从添加任务到图片落盘的完整下载流程。
- 验证时显示真实浏览器；其他阶段浏览器保持隐藏。
- 能生成不依赖目标机器开发环境的便携包。

## 2. 推荐栈与两条线路

### 2.1 主线路：Rod

- `github.com/go-rod/rod`
- `github.com/go-rod/stealth`
- 直接连接包内真实 Chromium 的 CDP。
- 默认配置：`"engine": "rod"`。

### 2.2 备用线路：Playwright-Go

- `github.com/mxschmitt/playwright-go`
- 使用同一个真实 Chromium，通过 `ConnectOverCDP` 连接。
- 便携版使用 `runtime\playwright\driver` 内置的 Node.js 和 Playwright 驱动。

两条线路不是两套下载器。浏览器控制层不同，但任务流程、图片保存、速度、错误处理和 UI 完全共用。

## 3. 浏览器模型

### 3.1 真实浏览器启动

`myreading` 使用普通有头 Chromium，而不是改变渲染范围的设备模拟窗口：

- 启动时把窗口放到桌面外，并在连接 CDP 前隐藏原生窗口。
- 保留真实屏幕尺寸和普通浏览器视口。
- 不添加传统自动化窗口样式。
- 关闭后台定时器和遮挡窗口节流，保证窗口隐藏后页面仍能工作。

### 3.2 验证流程

1. 使用任务 profile 创建 Chromium。
2. 隐藏窗口并进入目标页面。
3. 通过标题、DOM 和验证控件识别验证状态。
4. 需要验证时调用 `SetVisible(true)`，恢复并最大化同一个窗口。
5. 每 2 秒读取一次当前页面状态。
6. 验证成功后调用 `SetVisible(false)`。
7. 继续分页和图片采集，不再向用户显示浏览器。
8. 成功状态更新到 `verified-base`，供后续任务复用。

### 3.3 浏览器路径优先级

`configuredChromiumPath()` 当前顺序：

1. `runtime\chromium\chrome.exe`，即便携包内浏览器。
2. 开发机原项目 Chromium。
3. UI 保存路径。
4. 系统浏览器探测。

便携包的 EXE 因此明确指向包内 `runtime\chromium\chrome.exe`。

## 4. 页面与图片采集

### 4.1 页面采集

- `Snapshot()` 打开阅读页。
- `myreadingDOMSnapshotJS` 提取标题、图片、下一页和验证标记。
- `myreadingLoadImagesJS` 滚动页面并触发懒加载。
- URL 规范化和去重后合并多页图片。
- 下一页必须属于同一作品路径，避免跨作品采集。

### 4.2 图片获取顺序

1. 检查最终文件是否已经存在且可以正常解码。
2. 优先由浏览器重新加载原图并读取响应体。
3. 浏览器读取失败时使用带 Cookie、Referer、User-Agent 的 HTTP 客户端重试。
4. HTTP 返回非 2xx、HTML、JSON 或无效图片时判定失败。

### 4.3 保存与续传

- 文件名：`0001.jpg`、`0002.png` 等。
- HTTP 数据先写入 `<文件名>.part`。
- 使用 Go 图片解码器检查格式、宽度和高度。
- 校验通过后原子重命名为最终文件。
- 已完成文件在重试时直接复用；无效旧文件会被删除并重新下载。

## 5. 实时下载速度

实时速度统计的是网络传输字节，而不是磁盘中已有文件的大小。

### 5.1 三条字节来源

- Rod：监听当前图片 requestId 的 CDP `Network.dataReceived`。
- Playwright：监听当前图片 requestId 的 CDP `Network.dataReceived`。
- HTTP 回退：使用 `transferCountingWriter` 统计 `io.Copy` 的每次写入。

### 5.2 UI 采样

- 下载启动时显示 `实时 计算中`。
- 字节到达时立即更新累计值。
- 独立采样器每 500ms 刷新一次 UI。
- 当前采样区间没有新增字节时显示 `实时 0 B/s`。
- 传输恢复后根据新增字节和时间重新计算速率。
- 下载结束后停止并等待采样协程退出。
- `downloadSpeedTracker` 使用互斥锁保护并发访问。

## 6. 资源回收

所有任务退出路径均覆盖以下资源：

- 取消任务 context。
- 关闭页面、浏览器和 Playwright 驱动。
- 等待或终止 Chromium 父进程。
- 按精确 profile 路径清理 Chromium 子进程树。
- 删除任务临时 profile。
- 清理 `.part` 文件。
- 从活动 worker 表注销任务。

Rod、Playwright 和 Chromium 的 `Close()` 使用 `sync.Once`，允许重复调用。

`verified-base` 是验证状态基线，不属于泄漏资源，正常保留。

## 7. 版本规则

版本号由 `build.ps1` 在构建时生成并通过链接器写入：

```text
yyyyMMddHHmmss
```

规则为本地年月日时分秒、固定 14 位数字。无效注入值会回退为 `00000000000000`。

## 8. 便携包

当前便携包：

```text
dist\Comic_PC_Portable_20260812105826.zip
```

包含：

- 主程序及配置。
- Chromium 153 完整运行目录。
- Playwright 1.61.1 驱动与 Node.js。
- Rod 所需逻辑已编译进 EXE。
- UI 字体和图标资源。
- `runtime`、`download`、`logs`、`.tmp` 等可写目录。
- `manifest-sha256.json` 完整性清单。

目标机器不需要预装 Go、Node.js、Python、Playwright、Chrome 或 Edge。

## 9. 构建、测试与打包

```powershell
go test ./... -count=1
go test -race ./... -count=1
go vet ./...
.\build.ps1
.\package-portable.ps1 -Version <build.ps1 输出的14位版本号>
```

浏览器双线路冒烟测试使用 `MYREADING_BROWSER_SMOKE=1` 和 `MYREADING_BROWSER_BIN` 指向待验证 Chromium。

## 10. 当前完成状态

- [x] Go Rod 下载线路。
- [x] Go Playwright 下载线路。
- [x] UI 复用。
- [x] 真实 Chromium 验证窗口。
- [x] 验证完成后后台隐藏。
- [x] 浏览器优先图片读取与 HTTP 回退。
- [x] 图片校验和断点续传。
- [x] 实时下载速度和停滞零速。
- [x] 版本号规则。
- [x] 资源回收及重复关闭保护。
- [x] 自包含便携包。
- [x] Rod、Playwright、竞态、便携环境验证。

## 11. 主要验证记录

- `artifacts\verification-version-speed-cleanup.txt`
- `artifacts\portable-verification.txt`
- `artifacts\version-speed-cleanup.patch`
- `rollback-version-speed-cleanup.ps1`
